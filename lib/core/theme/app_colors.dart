import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // --- Primary Brand ---
  static const Color primary        = Color(0xFF2563EB);  // blue
  static const Color primaryLight   = Color(0xFFEFF6FF);  // light blue bg
  static const Color primarySurface = Color(0xFFDEEBFF);  // avatar bg

  // --- Semantic ---
  static const Color success        = Color(0xFF16A34A);
  static const Color successLight   = Color(0xFFF0FDF4);
  static const Color successSurface = Color(0xFFDCFCE7);
  static const Color warning        = Color(0xFFF97316);
  static const Color warningLight   = Color(0xFFFFF7ED);
  static const Color error          = Color(0xFFEF4444);
  static const Color errorLight     = Color(0xFFFFF5F5);
  static const Color errorSurface   = Color(0xFFFECACA);

  // --- Accent (for group icons, tags) ---
  static const Color purple         = Color(0xFF9333EA);
  static const Color purpleLight    = Color(0xFFF3E8FF);
  static const Color amber          = Color(0xFFCA8A04);
  static const Color amberLight     = Color(0xFFFEF9C3);

  // --- Neutrals ---
  static const Color backgroundPage  = Color(0xFFF8FAFC);
  static const Color backgroundBlue  = Color(0xFFEFF6FF);
  static const Color surface         = Colors.white;
  static const Color border          = Color(0xFFE2E8F0);
  static const Color divider         = Color(0xFFE2E8F0);

  // --- Text ---
  static const Color textPrimary     = Color(0xFF0F172A);
  static const Color textSecondary   = Color(0xFF475569);
  static const Color textMuted       = Color(0xFF64748B);
  static const Color textHint        = Color(0xFF94A3B8);
  static const Color textDisabled    = Color(0xFFCBD5E1);

  // --- Schedule Grid ---
  static const Color scheduleFreeBg  = Color(0xFFF1F5F9);
  static const Color scheduleBusyBg  = Color(0xFFFCA5A5);
  static const Color scheduleLegFree = Color(0xFFFECACA);
}

